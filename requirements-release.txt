# Runtime dependency set used to audit/build Asset Viewer releases.
# Keep synchronized with pyproject.toml's runtime dependencies.
Pillow==12.3.0
waitress==3.0.2
watchdog==6.0.0
tomli==2.4.1; python_version < "3.11"
